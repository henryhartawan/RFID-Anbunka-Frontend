@{
	ViewData["Title"] = "Master Shift";
	string myurl = new ConfigurationBuilder().AddJsonFile("appsettings.json").Build().GetSection("Path:URL").Value;
}

<style>
	.k-dropdown-wrap .k-input, .k-numeric-wrap .k-input, .k-picker-wrap .k-input {
		height: 30px;
	}
</style>

<div class="page-main">
	<div class="page-content padding-5">
		<div id="grid" style="margin-top:5px; margin-bottom:5px;"></div>
	</div>
</div>

<div class="modal fade modal-primary" id="editor" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
	<div class="" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<span aria-hidden="true">&times;</span>
				</button>
				<h4 class="modal-title" id="myModalTitle">Master Shift</h4>
			</div>
			<div class="modal-body margin-bottom-0 padding-bottom-0">
				<div class="panel">
					<div class="panel-body">
						<form class="form-horizontal">
							<div class="col-md-12">
								<div class="form-group form-material col-md-6">
									<label class="col-sm-3 control-label">Plant</label>
									<div class="col-sm-9">
										<input type="text" class="" id="PlantID" style="width:100%" required />
									</div>
								</div>
								<div class="form-group form-material col-md-6" style="display:none;">
									<label class="col-sm-3 control-label">Shift Name</label>
									<div class="col-sm-9">
										<input type="text" class="form-control" id="ShiftName" />
									</div>
								</div>
							</div>
							<div class="col-md-12">
								<div class="form-group form-material col-md-6">
									<label class="col-sm-3 control-label">Day Shift Start</label>
									<div class="col-sm-9">
										<input type="text" class="" id="DayShiftStart" style="width:100%" required />
									</div>
								</div>
								<div class="form-group form-material col-md-6">
									<label class="col-sm-3 control-label">Day Shift End</label>
									<div class="col-sm-9">
										<input type="text" class="" id="DayShiftEnd" style="width:100%" required />
									</div>
								</div>
							</div>
							<div class="col-md-12">
								<div class="form-group form-material col-md-6">
									<label class="col-sm-3 control-label">Night Shift Start</label>
									<div class="col-sm-9">
										<input type="text" class="" id="NightShiftStart" style="width:100%" required />
									</div>
								</div>
								<div class="form-group form-material col-md-6">
									<label class="col-sm-3 control-label">Night Shift End</label>
									<div class="col-sm-9">
										<input type="text" class="" id="NightShiftEnd" style="width:100%" required />
									</div>
								</div>
							</div>
							<div class="clearfix"></div>
						</form>
					</div>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-default" data-dismiss="modal" id='clear'>Close</button>
				<button type="button" class="btn btn-primary" id="btnSave">Submit</button>
			</div>
		</div>
	</div>
</div>

<script>
	var ShiftID = ''; var IUType = 'I';

	var AppD = {
		attachEvents: function () {
			var o = this;
			o.dom.$btnSave.on('click', o.eventHandlers.onClickBtnSave.bind(o));

			o.dom.$grid.on('click', '#btnCreate', o.eventHandlers.onClickBtnCreate.bind(o));
			o.dom.$grid.on('click', '#btnExcel', o.eventHandlers.onClickBtnExcel.bind(o));

			o.dom.$grid.on('click', '.btnUpdate', o.eventHandlers.onClickBtnUpdate.bind(o));
			o.dom.$grid.on('click', '.btnDelete', o.eventHandlers.onClickBtnDelete.bind(o));
		},
		cacheDom: function () {
			var o = this;
			o.dom.$editor = $("#editor");
			o.dom.$grid = $("#grid");
			o.dom.inputs = {};
			o.dom.inputs.$inputPlantID = $("#PlantID");
			o.dom.inputs.$inputShiftName = $("#ShiftName");
			o.dom.inputs.$inputDayShiftStart = $("#DayShiftStart");
			o.dom.inputs.$inputDayShiftEnd = $("#DayShiftEnd");
			o.dom.inputs.$inputNightShiftStart = $("#NightShiftStart");
			o.dom.inputs.$inputNightShiftEnd = $("#NightShiftEnd");
			o.dom.$btnSave = $("#btnSave");
		},
		data: {},
		dom: {},
		eventHandlers: {
			onClickBtnCreate: function () {
				var o = this;
				ShiftID = '';
				IUType = 'I';
				o.dom.inputs.$inputShiftName.val('');
				o.dom.inputs.$inputPlantID.data("kendoDropDownList").value('');
				o.dom.inputs.$inputPlantID.data("kendoDropDownList").enable(true);
				o.dom.inputs.$inputDayShiftStart.data('kendoTimePicker').value('');
				o.dom.inputs.$inputDayShiftEnd.data('kendoTimePicker').value('');
				o.dom.inputs.$inputNightShiftStart.data('kendoTimePicker').value('');
				o.dom.inputs.$inputNightShiftEnd.data('kendoTimePicker').value('');

				o.dom.$editor.modal('show');
			},
			onClickBtnDelete: function (e) {
				var o = this;
				var di = o.dom.$grid.data('kendoGrid').dataItem($(e.target).closest('tr'));

				swal.fire({
					title: 'Delete?',
					text: "You won't be able to revert this!",
					icon: 'warning',
					showCancelButton: true,
					confirmButtonText: 'Yes'
				}).then((result) => {
					if (result.isConfirmed) {
						$.ajax({
							method: 'post',
							headers: { 'Content-Type': 'application/json', 'XApiKey': 'pgH7QzFHJx4w46fI~5Uzi4RvtTwlEXp' },
							url: "@myurl"+'MasterShift/DEL',
							dataType: 'text',
							data: JSON.stringify({
								ShiftID: di.ShiftID
							})
						}).done(function (data, textStatus, jqXHR) {
							if (data !== 'success') {
								swal.fire('', data, 'error');
								return;
							}
							swal.fire({ title: 'Success', text: 'Data deleted', icon: 'success', timer: 2000, showConfirmButton: false });
						}).fail(function (jqXHR, textStatus, errorThrown) {
							swal.fire('Error', (jqXHR.responseText !== '' ? ': ' + jqXHR.responseText : ''), 'error');
						}).always(function (data_or_jqXHR, textStatus, jqXHR_or_errorThrown) {
							o.dom.$grid.data('kendoGrid').dataSource.read();
						});
					}
				});
			},
			onClickBtnExcel: function () {
				var o = this;
				o.dom.$grid.data('kendoGrid').saveAsExcel();
			},
			onClickBtnUpdate: function (e) {
				var o = this;
				var di = o.dom.$grid.data('kendoGrid').dataItem($(e.target).closest('tr'));

				o.dom.inputs.$inputShiftName.val(di.ShiftName);
				o.dom.inputs.$inputPlantID.data("kendoDropDownList").value(di.PlantID);
				o.dom.inputs.$inputPlantID.data("kendoDropDownList").enable(false);
				o.dom.inputs.$inputDayShiftStart.data('kendoTimePicker').value(di.DayShiftStart);
				o.dom.inputs.$inputDayShiftEnd.data('kendoTimePicker').value(di.DayShiftEnd);
				o.dom.inputs.$inputNightShiftStart.data('kendoTimePicker').value(di.NightShiftStart);
				o.dom.inputs.$inputNightShiftEnd.data('kendoTimePicker').value(di.NightShiftEnd);

				IUType = 'U';
				ShiftID = di.ShiftID;
				o.dom.$editor.modal('show');
			},
			onClickBtnSave: function (e) {
				var o = this;
				var PlantID = $("#PlantID").data('kendoDropDownList').value();
				var ShiftName = $("#ShiftName").val();
				var DayShiftStart = kendo.toString(o.dom.inputs.$inputDayShiftStart.data('kendoTimePicker').value(), 'HH:mm');
				var DayShiftEnd = kendo.toString(o.dom.inputs.$inputDayShiftEnd.data('kendoTimePicker').value(), 'HH:mm');
				var NightShiftStart = kendo.toString(o.dom.inputs.$inputNightShiftStart.data('kendoTimePicker').value(), 'HH:mm');
				var NightShiftEnd = kendo.toString(o.dom.inputs.$inputNightShiftEnd.data('kendoTimePicker').value(), 'HH:mm');

				if(PlantID != '' && DayShiftStart != '' && DayShiftEnd != '' && NightShiftStart != '' && NightShiftEnd != ''){
					swal.fire({
						title: "Save?",
						icon: 'warning',
						showCancelButton: true,
						confirmButtonText: 'Yes'
					}).then((result) => {
						if (result.isConfirmed) {
							$.ajax({
								method: 'post',
								headers: { 'Content-Type': 'application/json', 'XApiKey': 'pgH7QzFHJx4w46fI~5Uzi4RvtTwlEXp' },
								url: "@myurl" + 'MasterShift/INS',
								dataType: 'text',
								data: JSON.stringify({
									ShiftID: ShiftID,
									ShiftName: ShiftName,
									PlantID: PlantID,
									IUType: IUType,
									DayShiftStart: DayShiftStart,
									DayShiftEnd: DayShiftEnd,
									NightShiftStart: NightShiftStart,
									NightShiftEnd: NightShiftEnd,
									UserLogin: "@Context.Session.GetString("PIC_ID")"
								})
							}).done(function (data, textStatus, jqXHR) {
								if (data !== 'success') {
									swal.fire('Error', data, 'error');
									return;
								}
								o.dom.$editor.modal('hide');
								swal.fire({ title: 'Success', text: 'Data saved', icon: 'success', timer: 2000, showConfirmButton: false });
							}).fail(function (jqXHR, textStatus, errorThrown) {
								swal.fire('Error', (jqXHR.responseText !== '' ? ': ' + jqXHR.responseText : ''), 'error');
							}).always(function (data_or_jqXHR, textStatus, jqXHR_or_errorThrown) {
								o.dom.$grid.data('kendoGrid').dataSource.read();
							});
						}
					});
				} else {
					swal.fire({ title: 'Warning', text: 'Please fill all fields', icon: 'warning' });
				}
			}
		},
		init: function () {
			var o = this;
			o.cacheDom();
			o.attachEvents();
			o.initElements();
		},
		initElements: function () {
			var o = this;

			o.dom.$grid.kendoGrid({
				excel: {
					fileName: "Master Shift.xlsx",
					allPages: true,
					filterable: true
				},
				dataSource: {
					transport: {
						read: function (options) {
							$.ajax({
								method: 'post',
								headers: { 'Content-Type': 'application/json', 'XApiKey': 'pgH7QzFHJx4w46fI~5Uzi4RvtTwlEXp' },
								url: "@myurl"+'MasterShift/INQ',
								dataType: 'text',
								data: JSON.stringify({
									UserLogin: "@Context.Session.GetString("PIC_ID")"
								})
							}).done(function (data, textStatus, jqXHR) {
								try {
									var result = JSON.parse(data);
								}
								catch (e) {
									swal.fire('Error', 'Error loading grid', 'error');
									options.success({ data: [] });
									return;
								}
								options.success(result);
							}).fail(function (jqXHR, textStatus, errorThrown) {
								swal.fire('Error', (jqXHR.responseText !== '' ? ': ' + jqXHR.responseText : ''), 'error');
								options.success({ data: [] });
							});
						}
					},
					schema: {
						model: {
							id: "ShiftID",
							fields: {
								ShiftID: { type: "string" }, 
								ShiftName: { type: "string" },
								PlantID: { type: "string" }, 
								DayShiftStart: { type: "string" }, 
								DayShiftEnd: { type: "string" }, 
								NightShiftStart: { type: "string" }, 
								NightShiftEnd: { type: "string" }, 
								LastUpdate: { type: "string" }, 
								UserUpdate: { type: "string" }, 
								Remarks: { type: "string" }

							}
						}
					},
					pageSize: 20
				},
				scrollable: true,
				sortable: true,
				resizable: true,
				reorderable: true,
				filterable: {
					extra: false,
					operators: {
						string: {
							contains: "Contains",
							eq: "Equal to",
							neq: "Not equal to"
						}
					}
				},
				pageable: {
					refresh: true,
					input: true,
					numeric: true,
					pageSizes: true,
					buttonCount: 5
				},
				columns: [
				@if (Context.Session.GetString("edit_master_shift") == "True")
				{
					<text>
					{
						width: 50,
						command: [{
							name: "Update",
							text: "<i class='fa fa-pencil-square-o btnUpdate' style='font-size:18px;color:#737373;'></i>"
						}]
					},
					</text>
				}
				@if (Context.Session.GetString("del_master_shift") == "True")
				{
					<text>
					{
						width: 50,
						command: [{
							name: "Delete",
							text: "<i class='fa fa-trash-o btnDelete' style='font-size:18px;color:#800000;'></i>"
						}]
					},
					</text>
				}
				{
					field: "PlantID",
					title: "Plant",
					width: 50
				},
				{
					field: "ShiftID",
					title: "Shift",
					width: 100,
					hidden: true
				},
				{
					field: "ShiftName",
					title: "Shift",
					width: 100,
					hidden: true
				},
				{
					field: "DayShiftStart",
					title: "Day Shift Start",
					width: 100,
				},
				{
					field: "DayShiftEnd",
					title: "Day Shift End",
					width: 100,
				},
				{
					field: "NightShiftStart",
					title: "Night Shift Start",
					width: 100,
				},
				{
					field: "NightShiftEnd",
					title: "Night Shift End",
					width: 100,
				},
				{
					title: "Last Update",
					columns: [
					{
						field: "UserUpdate",
						title: "User",
						width: 100
					},
					{
						field: "LastUpdate",
						title: "Date",
						width: 100
					}]
				}],
				toolbar: $("#template_toolbar").html(),
			});

			$("#PlantID").kendoDropDownList({
				dataValueField: "IdValue",
				dataTextField: "ViewValue",
				dataSource: {
					transport: {
						read: function (options) {
							$.ajax({
								method: 'post',
								headers: { 'Content-Type': 'application/json', 'XApiKey': 'pgH7QzFHJx4w46fI~5Uzi4RvtTwlEXp' },
								url: "@myurl" + 'DropDown/INQ',
								dataType: 'text',
								data: JSON.stringify({ Ftype: "Plant", Plant: "" })
							}).done(function (data, textStatus, jqXHR) {
								try {
									var result = JSON.parse(data);
								}
								catch (e) {
									swal.fire('Error', 'Error loading grid', 'error');
									options.success({ data: [] });
									return;
								}
								options.success(result);
							}).fail(function (jqXHR, textStatus, errorThrown) {
								swal.fire('Error', (jqXHR.responseText !== '' ? ': ' + jqXHR.responseText : ''), 'error');
								options.success({ data: [] });
							});
						}
					}
				}
			}).data("kendoDropDownList");

			o.dom.inputs.$inputDayShiftStart.kendoTimePicker({
				format: 'HH:mm',
				dateInput: true
			});

			o.dom.inputs.$inputDayShiftEnd.kendoTimePicker({
				format: 'HH:mm',
				dateInput: true
			});

			o.dom.inputs.$inputNightShiftStart.kendoTimePicker({
				format: 'HH:mm',
				dateInput: true
			});

			o.dom.inputs.$inputNightShiftEnd.kendoTimePicker({
				format: 'HH:mm',
				dateInput: true
			});
		}
	};

	$(function () {
		AppD.init();
	});

</script>

<script type="text/x-kendo-template" id="template_toolbar">
	<span class="k-pager-refresh" style="font-weight: bold">&nbsp;&nbsp;- Master Shift -&nbsp;&nbsp;</span>
	<div class="toolbar">
	@if (Context.Session.GetString("add_master_shift") == "True")
	{
		<button data-toggle="tooltip" title="Create" type="button" class="btn ink-reaction btn-floating-action btn-xs btn-warning" id="btnCreate">
			<i class="k-icon k-i-plus"></i>
		</button>
	}
		<button data-toggle="tooltip" title="Save as Excel" type="button" class="btn ink-reaction btn-floating-action btn-xs btn-warning" id="btnExcel">
			<i class="k-icon k-i-excel"></i>
		</button>
	</div>
</script>